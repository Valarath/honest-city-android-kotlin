package cz.city.honest.external.validation

import cz.city.honest.external.EndpointsUrl

class ValidationUrl {

    companion object{
        const val VALIDATION_PREFIX = "${EndpointsUrl.PUBLIC}/validation"
    }
}